name: Test Path
on: [push]
jobs:
  test-path:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Verify path
      run: |
        ls -l "./WhoKnows-backend"
        cat "./WhoKnows-backend/WhoKnows backend.cproj"

